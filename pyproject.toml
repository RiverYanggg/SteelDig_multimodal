[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "paper-extractor"
version = "0.1.0"
description = "Paper extraction workflow for text and multimodal analysis."
requires-python = ">=3.10"
dependencies = [
  "openai",
]

